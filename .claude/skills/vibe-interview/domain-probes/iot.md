# Expert-level probes — INSPIRATION ONLY, do not copy verbatim

- 핵심 제약이 배터리 수명, 연결 안정성, 대역폭, 단가 중 무엇인지에 따라 프로토콜 선택이 어떻게 달라지는가?
- MQTT와 CoAP 중 어느 쪽이 실제 현장 네트워크와 디바이스 등급에 맞고, 그 선택이 운영 복잡도를 어떻게 바꾸는가?
- 디바이스 프로비저닝은 공장 출하시 선탑재, 현장 QR 온보딩, 앱 연동 중 어떤 방식이 현실적인가?
- 인증서 rotation이 가능한 장치인지, 만료 시 현장 출동이 필요한지, 그 비용을 누가 감당하는가?
- 오프라인 구간에서 수집한 이벤트를 edge에서 버퍼링할지, 샘플링할지, 유실 허용할지 어느 수준으로 정하는가?
- OTA 업데이트 실패 시 rollback이 가능한 파티션 구조인지, 실패 복구 절차는 사용자가 이해할 수 있는가?
- 센서 교정(calibration)과 펌웨어 버전 불일치가 데이터 품질을 깨는 시점은 언제인가?
- edge inference와 cloud attribution 중 어느 쪽이 지연 시간과 비용 목표에 맞는가?
- 장치 수명 동안 비밀값 저장소가 안전한지, TPM/secure element가 없는 경우 어떤 완화책이 필요한가?
- 주기 전송과 이벤트 전송 중 어떤 패턴이 배터리와 분석 가치의 균형을 맞추는가?
- 현장 설치자가 네트워크 자격증명을 입력해야 하는지, 자동 가입이 가능한지, 설치 실패율에 미치는 영향은 무엇인가?
- 디바이스 아이덴티티를 사용자, 사이트, 자산 중 어디에 귀속시킬 때 운영이 가장 단순해지는가?
- 시간 동기화 실패가 계측 데이터 해석을 얼마나 왜곡하는가?
- 펌웨어/하드웨어 호환성 매트릭스를 누가 관리하고, unsupported 조합 발견 시 차단 정책은 무엇인가?
- 장치가 여러 리전에 배포될 때 데이터 주권, 원격 제어 지연, 장애 대응 체계는 어떻게 달라지는가?
- 목표 SLA가 장치 온라인율인지, 이벤트 수집 성공률인지, 제어 명령 완료율인지 먼저 구분되어야 하지 않는가?
- 디바이스 폐기 또는 소유권 이전 시 credential revoke와 데이터 보존 정책은 어떻게 처리하는가?
