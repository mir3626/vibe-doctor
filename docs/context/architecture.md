# Architecture Context

PROJECT NOT INITIALIZED.

This file is project-owned. Run `/vibe-init` before implementation so the Orchestrator can replace this placeholder with the current project's stack, runtime, data model, integration boundaries, and verification approach.

## Initial Notes

- Do not infer architecture from the upstream `vibe-doctor` template.
- Record only project-specific decisions here after initialization.
