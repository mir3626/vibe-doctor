# Product context

이 저장소는 특정 앱 기능이 아니라 **새 프로젝트마다 재사용할 수 있는 AI-first BASE 프로젝트**를 제공한다.

성공 기준:
- `git clone` 후 바로 같은 프로세스로 작업 시작 가능
- 사용자 역할과 AI 역할이 분리되어 있음
- 루트 메모리가 비대해지지 않음
- 테스트 / QA / 보고가 누락되지 않음
- vendor 교체 가능성이 열려 있음
