# QA policy

PROJECT NOT INITIALIZED.

This file is project-owned. Run `/vibe-init` before implementation so QA commands, smoke checks, acceptance evidence, and release criteria match the current project.

## Baseline

1. Run the narrowest relevant verification first.
2. Run typecheck/lint/build when the project provides those scripts.
3. Record any skipped verification with a concrete reason.
