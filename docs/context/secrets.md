# Secrets Policy

PROJECT NOT INITIALIZED.

This file is project-owned. Run `/vibe-init` before implementation so secret handling, environment variables, provider credentials, and deployment-specific rules match the current project.

## Baseline

- Do not commit `.env`, `.env.*`, `secrets/**`, or credential files.
- Keep real credentials in local environment variables, OS/provider login stores, or gitignored local config.
- Document required environment variable names here after initialization, without storing secret values.
