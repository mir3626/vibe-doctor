# Token / Cost Policy

PROJECT NOT INITIALIZED.

This file is project-owned. Run `/vibe-init` before implementation so token, cost, logging, and context-retention rules match the current project and provider choices.

## Baseline

- Keep long logs out of root memory.
- Summarize large provider outputs before adding them to durable context.
- Track provider usage only through project-owned runtime artifacts after initialization.
