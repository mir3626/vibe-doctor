# Review 6 — 2026-04-18

dogfood8 iter-4 MVP 첫 실전 run (Bucket 가계부 4 Sprint 자율 완주) 인계 기반 + iter-3 harness diet 결과 점검.

## Inputs loaded

- `.vibe/agent/handoff.md` (iter-3 N3 passed 스냅샷)
- `.vibe/agent/session-log.md` 최근 18 entries (config `review.recentEntries` 미설정 → helper 기본 resolution 사용)
- `.vibe/agent/sprint-status.json` passedSprintCount=6, open pendingRisks 1건 (`lightweight-audit-sprint-M-process-discipline` INFO — iter-2 carryover)
- `.vibe/agent/project-decisions.jsonl` (helper 로드)
- `docs/context/harness-gaps.md` — openHarnessGapCount=0
- `.vibe/audit/iter-3/rules-deleted.md` — pendingRestorations 4건 (`two-tier-audit-convention` B + tier-C 3건)
- `git log --oneline -25` — 이전 `review-*.md` 없음, 기본 20 commits 범위 (iter-3 kickoff ~ dashboard iOS glass merge)
- **외부 입력** (handoff prompt, helper 범위 밖):
  - dogfood8 인계 프롬프트 본문 (7 friction, 사용자 제공)
  - `app/` 디렉터리는 downstream product 이므로 upstream sprint-status 에 미반영 (separate run)

## Findings (severity desc)

### Auto-seeded (detectOptInGaps + pendingRestorations)

detectOptInGaps 는 upstream (CLI 하네스) 대상이므로 bundle/browserSmoke web-platform 조건 매칭 없음 → 0건. `#5` (downstream web 프로젝트에서 opt-in 불가) 는 별도 friction finding 으로 기록.

```yaml
- id: review-pending-restoration-two-tier-audit-convention
  severity: friction
  priority: P2
  proposal: "'Two-tier audit convention' 복원 여부 결정 필요 (tier=B, reason=incident_count=0, gap-rule-only-in-md coverage, source=.vibe/audit/iter-3/rules-deleted.md)"
  estimated_loc: 0
  proposed_sprint: backlog

- id: review-pending-restoration-실패-에스컬레이션
  severity: friction
  priority: P3
  proposal: "'실패 에스컬레이션' 복원 여부 결정 필요 (tier=C, reason=incident_count=0, no gap-* coverage, source=.vibe/audit/iter-3/rules-deleted.md)"
  estimated_loc: 0
  proposed_sprint: backlog

- id: review-pending-restoration-항상-지킬-것
  severity: friction
  priority: P3
  proposal: "'항상 지킬 것 (세션 시작 시 반드시 확인)' 복원 여부 결정 필요 (tier=C, reason=incident_count=0, duplicated by preserved role constraints and sprint flow, source=.vibe/audit/iter-3/rules-deleted.md)"
  estimated_loc: 0
  proposed_sprint: backlog

- id: review-pending-restoration-필요할-때만-읽을-문서
  severity: friction
  priority: P3
  proposal: "'필요할 때만 읽을 문서' 복원 여부 결정 필요 (tier=C, reason=incident_count=0, no gap-* coverage, source=.vibe/audit/iter-3/rules-deleted.md)"
  estimated_loc: 0
  proposed_sprint: backlog
```

- 근거: iter-3 N1 에서 tier B/C 로 삭제 후 replay 시 incident 재발 0 인 상태가 ≥1 iteration 지속. `two-tier-audit-convention` 만 `gap-rule-only-in-md` 가 아직 열려 있어 복원 후보로 가장 강함. 나머지 3건은 재발 근거 전무 → delete 확정 수순 제안.

### Blocker (agent-mode)

```yaml
- id: review-interview-success-metric-stuck
  severity: blocker
  priority: P0
  proposal: "interview engine 의 success_metric coverage 계산을 sub-field 가중치 기반으로 교정 (key_metrics / measurement_scheme 에 confidence 0.95+ 재답변 시 coverage 가 누적되지 않는 버그). soft-terminate 임계 재점검 포함."
  estimated_loc: 80
  proposed_sprint: iter-4 slot-1
```

- priority_score 10·agent(1) + 5·token(1) + 1·user(0) = 15. ambiguity 0.218 로 soft-terminate 임계(0.3) 이하인데도 coverage 미충족으로 빠져나올 수 없어 수동 `--abort` 로 탈출. 가중치 산정 로직이 sub-field 재답변을 반영하지 못하는 회계 버그로 추정. agent-mode 재현성 파괴 이슈라 P0 확정.
- 원 인계 `#1` (permission preset literal-match) 은 사용자 확인 결과 harness friction 이 아닌 accidental interrupt 후속 현상으로 판정 → 삭제.

### Friction (agent 우회 가능)

```yaml
- id: review-audit-skip-set-hard-fail
  severity: friction
  priority: P1
  proposal: "scripts/vibe-audit-skip-set.mjs 가 .vibe/config.local.json 미존재 시 hard-fail 하지 않고 bootstrap (기본 스켈레톤 생성 + auditSkippedMode 세팅). prototype 면제 경로 복구."
  estimated_loc: 25
  proposed_sprint: iter-4 slot-2

- id: review-preflight-roadmap-iteration-boundary
  severity: friction
  priority: P1
  proposal: "preflight.planner.presence 가 roadmap 의 iteration 경계 (## Iteration iter-N) 를 인식하도록 파싱 수정. currentIteration 이후 섹션만 스캔해서 next sprint 매칭."
  estimated_loc: 60
  proposed_sprint: iter-4 slot-2

- id: review-bundle-browsersmoke-path-configurable
  severity: friction
  priority: P1
  proposal: ".vibe/config.json.bundle.path / browserSmoke.dist 를 configurable (기본 'dist', app/ 하위 프로덕트는 'app/dist'). Phase 3 Step 3-4 opt-in 시 product 경로 질의 추가."
  estimated_loc: 70
  proposed_sprint: iter-4 slot-2

- id: review-planner-spec-component-contract
  severity: friction
  priority: P2
  proposal: "Planner spec 체크리스트에 'Toaster/ToastProvider 를 root 에 mount', '이벤트 핸들러 null-safe' 같은 component integration 계약을 공용 checklist 로 추가 (dogfood8 B3·B4 에서 Codex 2회 재위임 근본 원인)."
  estimated_loc: 30
  proposed_sprint: iter-4 slot-3
```

- `#3` — script hard-fail 은 prototype workflow 와 직접 충돌. `resolvePendingRisk()` 우회는 1회성으로 허용되나 재현성 붕괴. iter-3 rules-deleted `two-tier-audit-convention` 복원과 같이 묶어서 판단하면 LOC 회수도 맞음.
- `#4` — preflight WARN 이 iter 경계 이후에도 iter-1 M1 섹션을 "next sprint" 로 오인. dogfood8 94분 내내 계속 WARN, script-wrapper 로 충분히 해결 가능 (iteration 경계 파싱).
- `#5` — upstream CLI 는 영향 없지만 `app/` downstream (web/mobile) 은 bundle-size gate 를 opt-in 조차 못 함. Phase 3 Step 3-4 의 path configurable 이 root cause. agent-mode 에서 web 프로덕트를 실제로 돌릴 때마다 blocker 화됨.
- `#7` — B3 TagEditor null-event + B4 recompute-toast 누락은 Codex 산출물의 component integration 누락 패턴. Planner spec 의 공용 체크리스트에 포함시키면 iter-4 이후 재발 방지.

### Polish

```yaml
- id: review-actualloc-lockfile-exclude
  severity: polish
  priority: P3
  proposal: "vibe-sprint-commit.mjs actualLoc 집계에서 package-lock.json / pnpm-lock.yaml / yarn.lock 를 기본 제외 (scope glob 대신 lockfile blacklist). +150 LOC freeze budget noise 제거."
  estimated_loc: 20
  proposed_sprint: iter-4 slot-3
```

- `#6` — B2 net +4303 의 대부분이 lockfile diff 로 LOC 게이트 판정 오염. 예외 목록은 일반적 convention 이라 script-wrapper 한 방으로 해결.

## Suggested next-sprint scope

iter-4 growth budget **net ≤ +150 LOC / 0 new scripts** 유지 전제 + soft freeze 진입 경로 `/vibe-review findings` 정당화 적용.

- **slot-1 (agent-mode unblock, P0)**: `#2` interview success_metric coverage 재계산. 추정 +80 LOC. agent-mode E2E 재현성이 본 iter 의 핵심 가치 → 가장 먼저.
- **slot-2 (script-wrapper triage, P1 ×3)**: `#3` audit-skip-set bootstrap + `#4` preflight iteration 경계 + `#5` bundle/browserSmoke path configurable. 추정 +155 LOC (freeze budget 약간 상회 → slot-2 내에서 `#5` 를 defer 하거나 `#3` 를 최소 패치로 축소).
- **slot-3 (Planner 계약 + polish)**: `#7` Planner spec component integration 체크리스트 + `#6` actualLoc lockfile exclude. 추정 +50 LOC.
- **backlog (pending restoration 4건)**: `two-tier-audit-convention` 는 `gap-rule-only-in-md` 해제 조건으로 slot-2 `#3` 와 묶어 재평가. 나머지 tier-C 3건 은 iter-4 종료 시 delete 확정 또는 추가 1 iter 관찰.

recommended_approach 우선순위: `script-wrapper > md-rule > config-default > user-action` — slot-1 전량 script, slot-2 전량 script-wrapper, slot-3 은 md-rule(`#7`) + script(`#6`). 모든 findings 가 `user-action` 아래 계층에서 해소 가능 → soft-freeze 방향성과 일치.

## Links

- `.vibe/agent/handoff.md`
- `.vibe/agent/session-log.md`
- `.vibe/agent/sprint-status.json`
- `.vibe/audit/iter-3/rules-deleted.md`
- `.vibe/audit/iter-3/dogfood8-handoff-prompt.md` (upstream 에 커밋된 전달 프롬프트)
- `docs/context/harness-gaps.md` (openHarnessGapCount=0, 본 review 에는 ledger 인용 불필요)
- `.vibe/interview-log/iter-4-budget-app.json` (dogfood8 PO-proxy transcript, 외부 run 산출물)
- dogfood8 handoff prompt (사용자 제공 텍스트, 7 friction raw evidence)
