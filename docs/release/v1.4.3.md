# v1.4.3 (2026-04-XX)

## iter-6 M2 - parser false-positive regression fix

- `collectPendingRestorationDecisions` now excludes rule slugs marked `delete-confirmed` in post-decision sections of `.vibe/audit/iter-*/rules-deleted.md`, removing the dogfood9 review-14 restoration false positives.
- `scripts/vibe-sprint-complete.mjs` now finds iter-1 style roadmap `- **id**:` bullets beyond the old six-line lookahead, reducing `warning=roadmap-id-missing` noise.
- Added regression coverage in `test/vibe-review-inputs.test.ts` and `test/sprint-commit.test.ts`.

(M3 status-tick Windows regression will append to this document in a follow-up Sprint.)
