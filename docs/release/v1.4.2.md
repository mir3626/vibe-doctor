# v1.4.2 (2026-04-19)

## iter-4 O2 script-wrapper triage

- `vibe-audit-skip-set.mjs` now bootstraps a missing `.vibe/config.local.json` with a minimal `userDirectives` skeleton before applying audit skip state.
- `vibe-preflight.mjs` planner presence now scopes next-sprint detection to `.vibe/agent/iteration-history.json.currentIteration` when roadmap iteration sections exist, with legacy flat roadmap fallback preserved.
- Bundle-size config now prefers `bundle.path`, falls back to legacy `bundle.dir`, then defaults to `dist`.
- Browser smoke settings now expose optional `browserSmoke.dist` with `dist` as the default.
- Added regression coverage for audit-skip bootstrap, roadmap iteration resolution, and configurable dist path resolution.
