# v1.5.7

## Summary

Track Linux shell wrappers as executable in Git.

## Changes

- Marked `scripts/run-codex.sh` executable in the template index.
- Marked `.claude/statusline.sh` executable in the template index.

## Notes

v1.5.6 ensures sync writes `.sh` files with mode `0755`; this patch also records the intended executable mode in Git so downstream syncs do not leave mode-only diffs for shell wrappers.
