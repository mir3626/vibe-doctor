# v1.7.4 - Context Overhead Policy Checkpoint

Date: 2026-05-04

LTS: `v1.7.3-lts` remains the stable baseline. This release is a mainline policy checkpoint for future dogfood.

## Changes

- Recorded the approved context-overhead policy without adding capsule/router or prompt-reduction code.
- Clarified that `vibe-checkpoint` freshness does not prove all durable decisions were persisted.
- Documented durable-event markers for user directives, scope changes, risk lifecycle changes, skipped verification, release/tag/push/sync, harness policy decisions, blockers, scope expansion, and changed next action.
- Added `gap-context-overhead-policy` so future `/vibe-review` runs revisit dogfood evidence before any capsule/router prompt reduction.

## Non-goals

- No capsule or router implementation.
- No prompt injection reduction.
- No new fail-closed context coverage gate.
- No new public harness script.

## Verification

- `npm run typecheck`
- `npm test`
- `npm run build`
