# MCP-004 — Tool Metadata and Model Visibility

## Goal

Make tool discovery, confirmation, and selection explicit.

## Work

- add annotations to every tool;
- add exact output schemas;
- explicitly set model/app visibility;
- rewrite descriptions with `Use this when`;
- add invocation status strings where useful;
- add deterministic catalog audit and snapshot.

## DoD

Raw `tools/list` matches the approved classification matrix.
